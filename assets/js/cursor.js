// export function cursorEffect() {
//     const gradientCursor = document.getElementById("gradientCursor");
//     const gradientCursorInner = document.getElementById("gradientCursorInner");

//     if (!gradientCursor || !gradientCursorInner) return;

//     document.addEventListener("mousemove", (e) => {
//         gradientCursor.style.left = `${e.clientX}px`;
//         gradientCursor.style.top = `${e.clientY}px`;
//         gradientCursorInner.style.left = `${e.clientX}px`;
//         gradientCursorInner.style.top = `${e.clientY}px`;
//     });
// }